import { BoldFirstFourPipe } from './bold-first-four.pipe';

describe('BoldFirstFourPipe', () => {
  it('create an instance', () => {
    const pipe = new BoldFirstFourPipe();
    expect(pipe).toBeTruthy();
  });
});
